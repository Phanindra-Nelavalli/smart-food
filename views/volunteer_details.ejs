<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Volunteer Details</title>
    <link rel="stylesheet" href="/static/details.css" />
  </head>
  <body>
    <%-include('nav')-%>
    <main>
      <h2>Volunteer Details</h2>

      <div class="card-container">
        <% volunteers.forEach(volunteer => { %>
        <div class="card">
          <img
            src="https://via.placeholder.com/100"
            alt="<%= volunteer.name %>"
            class="profile-pic"
          />
          <div class="name"><%= volunteer.name %></div>
          <div class="details">Phone Number: <%= volunteer.number %></div>
          <div class="details">Email: <%= volunteer.email %></div>
          <div class="details">Date of Birth: <%= volunteer.dobString %></div>
        </div>
        <% }) %>
      </div>
    </main>
  </body>
</html>
